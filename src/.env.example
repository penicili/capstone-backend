# API Configuration
API_VERSION=1.0.0
API_TITLE=Capstone KPI & ML API
API_DESCRIPTION=Backend API for KPI dashboard and ML predictions

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=True

# ML Directory (relative to src/)
ML_DIR=assets/models

# ML Model Paths (relative to ML_DIR)
FINAL_GRADE_MODEL_PATH=final_grade_model.pkl
DROPOUT_MODEL_PATH=dropout_model.pkl
LABEL_ENCODER_FINALGRADE_PATH=label_encoder_finalgrade.pkl
LABEL_ENCODER_DROPOUT_PATH=label_encoder_dropout.pkl

# Logging
LOG_LEVEL=INFO
LOG_FILE=app.log

# Config database buat OLAP
DB_HOST=localhost
DB_PORT=3006
DB_USER=root
DB_PASSWORD=
DB_NAME=capstone_kpi

# Database Pool Configuration
DB_POOL_MIN_CACHED=2
DB_POOL_MAX_CACHED=5
DB_POOL_MAX_CONNECTIONS=10

# Caching
KPI_CACHE_TTL_SECONDS=300